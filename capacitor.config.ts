import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eseyam',
  appName: 'E-Seyam',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
